#version 450

layout (local_size_x=16, local_size_y=16) in;

layout(set=0, binding=0) uniform ConfigBuffer {
    vec4 color;
};

layout(set=1, binding=0, rgba8) writeonly uniform image2D fill;

void main() {
    imageStore(fill, ivec2(gl_GlobalInvocationID.xy), color);
}